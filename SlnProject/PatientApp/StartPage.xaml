<Page x:Class="PatientApp.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Welkom">

    <Page.Resources>
        <Style x:Key="PrimaireKnop" TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Rand" Background="{TemplateBinding Background}"
                                CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Rand" Property="Background" Value="#1A6E9A" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Rand" Property="Background" Value="#083D58" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <!-- Gradient achtergrond (teal) -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#082C3E" Offset="0" />
                <GradientStop Color="#0C3D55" Offset="0.5" />
                <GradientStop Color="#154E6E" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Gecentreerde witte kaart -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="White" CornerRadius="14"
                Padding="52,42,52,42" Width="460">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.20"
                                  BlurRadius="30" ShadowDepth="6" Direction="270" />
            </Border.Effect>

            <StackPanel>
                <!-- Logo + naam -->
                <TextBlock Text="🏥" FontSize="52" HorizontalAlignment="Center" Margin="0,0,0,12" />
                <TextBlock Text="DoktorsPortaal" FontSize="28" FontWeight="Bold"
                           Foreground="#0C3D55" HorizontalAlignment="Center" Margin="0,0,0,4" />
                <TextBlock Text="Patiënt applicatie" FontSize="14" Foreground="#64748B"
                           HorizontalAlignment="Center" Margin="0,0,0,28" />

                <!-- Scheidingslijn -->
                <Border Height="1" Background="#E2E8F0" Margin="0,0,0,24" />

                <!-- Functies -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="34" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="📅" FontSize="17" VerticalAlignment="Top" Margin="0,1,0,0" />
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Mijn afspraken" FontWeight="SemiBold" Foreground="#1E293B" FontSize="14" />
                        <TextBlock Text="Bekijk al uw afspraken en annuleer toekomstige afspraken."
                                   Foreground="#64748B" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="34" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="➕" FontSize="17" VerticalAlignment="Top" Margin="0,1,0,0" />
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Nieuwe afspraak" FontWeight="SemiBold" Foreground="#1E293B" FontSize="14" />
                        <TextBlock Text="Boek een afspraak bij een dokter op een beschikbaar tijdstip."
                                   Foreground="#64748B" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="34" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="👤" FontSize="17" VerticalAlignment="Top" Margin="0,1,0,0" />
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Mijn profiel" FontWeight="SemiBold" Foreground="#1E293B" FontSize="14" />
                        <TextBlock Text="Beheer uw persoonlijke gegevens en profielfoto."
                                   Foreground="#64748B" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <!-- CTA knop -->
                <Button Content="Aanmelden" Click="BtnAanmelden_Click"
                        HorizontalAlignment="Stretch" Padding="0,14"
                        Background="#0C3D55"
                        Style="{StaticResource PrimaireKnop}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
